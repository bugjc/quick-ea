
spring.jackson.serialization.INDENT_OUTPUT=true

spring.jpa.hibernate.ddl-auto=create
spring.datasource.url=jdbc:mysql://mysql:3306/ea_jwt?useUnicode=true&characterEncoding=utf-8
spring.datasource.driverClassName=com.mysql.jdbc.Driver
spring.datasource.username=jwt
spring.datasource.password=jwt2018
spring.jpa.database-platform=org.hibernate.dialect.MySQL57Dialect

jwt.header=Authorization
jwt.secret=mySecret
jwt.expiration=60
jwt.route.authentication.path=/auth
jwt.route.authentication.refresh=/refresh
jwt.route.authentication.verify=/verify


# eureka:off
eureka.instance.hostname=${spring.application.name}
eureka.client.serviceUrl.defaultZone=http://eureka:123456@eureka1-server:8000/eureka/,http://eureka:123456@eureka2-server:8001/eureka/
# eureka:on
